import React from "react";
import { StyleSheet, Text, View } from "react-native";

const TimeTemplate = () => {
  return (
    <View>
      <Text>TimeTemplate</Text>
    </View>
  );
};

export default TimeTemplate;

const styles = StyleSheet.create({});
